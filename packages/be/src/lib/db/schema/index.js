import { users } from './users.js';
import { roles } from './roles.js';
import { passwordResetRequests } from './password-reset-requests.js';

export const schema = {
	users,
	roles,
	passwordResetRequests
};

export const relations = {};
